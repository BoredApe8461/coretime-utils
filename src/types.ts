export type Timeslice = number;
export type CoreIndex = number;

export type Balance = number;

export const Id = {
  _enum: {
    U8: 'u8',
    U16: 'u16',
    U32: 'u32',
    U64: 'u64',
    U128: 'u128',
  },
};
